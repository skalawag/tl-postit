<%= render "shared/content_title", title: "Add a Category" %>

<%= render partial: "shared/error", object: @category %>

<%= form_for @category do |f| %>
  <%= f.label :category %>
  <%= f.text_field :category %>
  <br/>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
